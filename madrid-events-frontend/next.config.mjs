/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.madrid.es', 'a.tile.openstreetmap.org', 'b.tile.openstreetmap.org', 'c.tile.openstreetmap.org', 'www.tea-tron.com'], // Agrega aquí los dominios de imágenes permitidos
  },
};

export default nextConfig;
